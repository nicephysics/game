import { category, config } from "../config/config.js"

import { things } from "./things.js"

if (true) {
  // 2 space indent!
}

export class ThingStat {
  // static
  
  // fields
  
  // constructor
  constructor(thing) {
    
  }
  
}
